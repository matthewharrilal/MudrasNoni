{
  "name": "mudrasnoni",
  "version": "1.0.0",
  "description": "## 🎯 **PROJECT OVERVIEW**",
  "main": "script.js",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html --watch=.",
    "start": "live-server --port=3000 --open=/index.html --watch=.",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
